import React from 'react';
export interface PaginationProps {
    pageInfo: WPGraphQL.WpPageInfo;
    baseURL: string;
    nextLabel?: string;
    nextAriaLabel?: string;
    previousLabel?: string;
    previousAriaLabel?: string;
    ariaLabel?: string;
    NextPage?: React.ComponentType<PageNavigationProps>;
    PreviousPage?: React.ComponentType<PageNavigationProps>;
}
export interface PageNavigationProps {
    baseUrl: string;
    cursor: string;
    ariaLabel: string;
    label: string;
}
export declare function NextPageNavigation({ baseUrl, cursor, ariaLabel, label, }: PageNavigationProps): JSX.Element;
export declare function PreviousPageNavigation({ baseUrl, cursor, ariaLabel, label, }: PageNavigationProps): JSX.Element;
export declare function Pagination({ nextLabel, nextAriaLabel, previousLabel, previousAriaLabel, ariaLabel, pageInfo, baseURL, NextPage, PreviousPage, }: PaginationProps): JSX.Element | null;
