import React from 'react';
import { UriInfo } from '../../types';
export interface Template {
    default: React.ComponentType;
}
export interface Templates<T extends Template> {
    index: Promise<T> | T;
    [template: string]: Promise<T> | T;
}
export declare function resolveTemplate<T extends Template>(pageInfo: UriInfo | undefined, templates: Templates<T>): Promise<T>;
export declare function TemplateLoader<T extends Template>({ templates, uriInfo, dynamicLoader, }: {
    uriInfo: UriInfo | undefined;
    templates: Templates<T>;
    dynamicLoader: (loader: () => Promise<T>) => React.ComponentType;
}): JSX.Element | null;
