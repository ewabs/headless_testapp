import React from 'react';
import type { MenuItem } from './Menu';
interface MenuListProps {
    items?: MenuItem[];
    anchor?(item: MenuItem): React.ReactNode;
}
export declare const MenuList: ({ items, anchor, }: MenuListProps) => JSX.Element[] | null;
export {};
